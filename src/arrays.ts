let arr: Array<number> = [];
let ar: number[] = [];

type User = {
  name: string;
  isActive: boolean;
};

let allUsers: Array<User> = [];

allUsers.push({ name: "", isActive: true });
const MLModels: number[][] = [
    [255, 255, 255],
    []
]

export {};